[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production]
  environment = { NODE_VERSION = "20", NODE_ENV = "development", NPM_FLAGS = "--legacy-peer-deps" }
